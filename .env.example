# 本地 `npm run dev`：Vite 會把 /api 轉發到此網址（避免 CORS）
# 填「origin」即可，不要結尾斜線。建議改專案內建好的 `.env.development`，
# 或新增不進版控的 `.env.development.local` 覆寫。
#
# UAT 範例（以文件為準）：https://line.uat.sport115ntp.aitago.tw
# VITE_API_PROXY_TARGET=https://line.uat.sport115ntp.aitago.tw
VITE_API_PROXY_TARGET=
